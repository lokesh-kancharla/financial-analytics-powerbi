# GitHub Linguist cleanup
# These .m files are Microsoft Power Query M, but GitHub may misclassify .m as Wolfram Language.
# Exclude them from repository language statistics while keeping the actual Power Query source files.
powerbi/queries/*.m -linguist-detectable
